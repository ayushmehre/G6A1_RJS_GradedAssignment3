import theme from "../styles/Theme";

export default class ITheme {
    fonts: any = theme.fonts;
    colors: any = theme.colors;
    padding: any = theme.padding;
    margin: any = theme.margin;
    screen: any = theme.screen;
}